# 🚀 React Frontend Application

This is the **React frontend** for the task management.  
It communicates with the Django backend through REST APIs and handles all UI/UX functionalities.

---



---

# ⚙️ Prerequisites

Before running this project, ensure you have:

- **Node.js ≥ 16**
- **npm ≥ 8** (or Yarn)
- Backend running at `http://127.0.0.1:8000/` or your deployed API

Check your versions:

```bash
node -v
npm -v


git clone https://github.com/muhammedkm10/tasXFrontEnd.git
npm install
